# FileSystemsExplorer

Minimal playable console game that explores the filesystem, with modules for filesystem navigation, traps, a simple UI, and storage including ranking and save/load.

Build: `make`
Run: `make run`
